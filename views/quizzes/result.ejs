
<h1>
    Resultado:
</h1>

<p>
    La respuesta <strong> <%= answer %> </strong> es <%= result ? 'Correcta' : 'Incorrecta' %>.
</p>

<p>
    <% if (!result) { %>
        <a href="/quizzes/<%= quiz.id %>/play?answer=<%= answer %>"> Inténtelo otra vez </a>
        
    <% if (!result) { %>  		//Esto solo aparece si la respuesta es incorrecta
       <a href="/quizzes/<%= quiz.id %>/play?answer=<%= answer %>"> Inténtelo otra vez </a>
    <% } %>
</p>

<a href="/goback"><button>Volver</button></a>
